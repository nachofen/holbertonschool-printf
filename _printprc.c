#include <stdio.h>
#include "main.h"

/**
 * _printprc - this function prints the char % and adds 1 to the counter
 * Return: updated counter
 */
int _printprc(void)
{
	int contador = 0;

	contador += 1;
	_putchar(37);
	return (contador);
}
